import React from 'react'

const Square = ({ value }) => {

    // console.log(props);
    return (
        <div>
            <button type="button">{ value }</button>
        </div>
    )
}

export default Square
